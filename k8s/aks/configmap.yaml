apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-gateway-config
  namespace: ai-on-k8s
data:
  MODEL_PROVIDER: "mock"
  MODEL_NAME: "demo-model"
  OLLAMA_HOST: "http://ollama:11434"
  VLLM_HOST: "http://vllm:8000"
