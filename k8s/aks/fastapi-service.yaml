apiVersion: v1
kind: Service
metadata:
  name: ai-gateway
  namespace: ai-on-k8s
spec:
  type: LoadBalancer
  selector:
    app: ai-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
