apiVersion: v1
kind: Service
metadata:
  name: ai-gateway
spec:
  type: NodePort
  selector:
    app: ai-gateway
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
